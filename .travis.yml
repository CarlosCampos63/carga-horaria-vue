language: node_js

node_js: 8

script: 
  - acho "$FIREBASE_TOKEN"

install:
  - npm install
  - npm install firebase-tools
  - npm run build

deploy:
  provider: firebase
  token:
    secure: FIREBASE_TOKEN

cache: npm