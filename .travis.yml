language: node_js

node_js: 8

script: 
  - echo "$FIREBASE_TOKEN"

install:
  - npm install
  - npm install -g firebase-tools
  - npm run build

deploy:
  provider: firebase
  token:
    secure: FIREBASE_TOKEN

cache: npm